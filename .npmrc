# supabase-js starts a realtime client even when nothing subscribes to it,
# and that client needs a global WebSocket. Node 22 has one; Node 20 has it
# only behind this flag. Setting it here means `npm run seed` works on
# Node 20 without anyone needing to know why.
node-options=--experimental-websocket
